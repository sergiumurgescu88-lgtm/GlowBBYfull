server {
    listen 80 default_server;
    listen [::]:80 default_server;
    server_name media.glowbby.online;
    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl default_server;
    listen [::]:443 ssl default_server;
    server_name media.glowbby.online;

    ssl_certificate /etc/letsencrypt/live/media.glowbby.online/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/media.glowbby.online/privkey.pem;

    location / {
        proxy_pass http://127.0.0.1:7865;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_read_timeout 300s;
    }
}
